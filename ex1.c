#include <stdio.h>

/* This is a comment. */
int main(int argc, char *argv[])
{
    int distance = 100;

    // this is also a comment
    printf("You are %d miles away.\n", distance);

    printf("The treasure is %d meters to the north.\n", distance);

    printf("There are %d miles until you arrive at your destination.\n", distance);

    printf("We have %d bottles of beer on the wall.\n", distance);

    printf("%d hours left before rescue.\n", distance);

    return 0;
}
